<%= simple_form_for [treatment, booking] do |f| %>
  <%= f.input :date, as: :date, html5: true %>
  <%= f.input :time, as: :time, html5: true %>
  <%= f.input :location %>
  <%= f.button :submit, class: "btn btn-my-beauty hvr-float-shadow" %>
<% end %>